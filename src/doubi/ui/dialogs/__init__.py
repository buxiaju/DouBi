"""GUI dialogs.

Each dialog is a factory-built QWidget / QDialog with all Qt
imports lazy-loaded inside the function, so the module is import-safe
without PySide6.
"""

from __future__ import annotations
